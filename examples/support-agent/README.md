# Support-agent example

The blocked candidate removes the approval step and expands a destructive refund schema.
The review candidate preserves the invariant but still changes a high-risk capability, so the
default policy requests human review.

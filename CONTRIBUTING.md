# Contributing

1. Open an issue describing the behavior, evidence, and intended compatibility impact.
2. Add or update deterministic tests before changing the decision engine.
3. Run `python scripts/check_release.py` before opening a pull request.
4. Never add a product claim without a reproducible artifact and documented scope.

Public schemas and exit-code semantics require a changelog entry.
